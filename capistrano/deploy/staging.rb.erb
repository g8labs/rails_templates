set :user, 'ubuntu'

set :use_sudo, false

set :ssh_options, port: 22, forward_agent: true

set :rails_env, 'staging'

set :branch, 'staging'

set :nginx_domains, '<%= @nginx_domains %>'

server '<%= @server_name %>', user: fetch(:user), roles: %w(web app db)
